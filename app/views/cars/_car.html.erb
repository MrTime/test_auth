<tr>
  <td><%= car.title %></td>
  <td><%= car.year %></td>
  <td><%= car.price.format %></td>
  <td><%= (car.details.map {|d| "#{d.kind}: #{d.value}"}).join(", ") %></td>
  <td><%= link_to 'Show', car %></td>
  <td><%= link_to 'Edit', edit_car_path(car) %></td>
  <td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
